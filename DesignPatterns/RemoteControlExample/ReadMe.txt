This example uses the Command pattern to command abstract home appliances. To use this example, run RemoteControlTest.class in
the terminal:

java RemoteControlTest

The remote control has a set of command slots that can be reprogrammed by swapping array slots using .setCommand() and the associated objects.
