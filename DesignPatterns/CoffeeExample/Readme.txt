To use this example run StarbuzzCoffee.class in the terminal:

Java StarbuzzCoffee

Although not easily apparent, the prices of each drink in the menu is determined by unpacking decorators.
The example can be modified by combining different condiments in the StarbuzzCoffee java file and recompiling.
