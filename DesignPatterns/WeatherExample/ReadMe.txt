An example of Observer pattern, will update weather displays with changes in virtual weather conditions.
Displays decide what to do with information given.
Can be run in terminal by doing:

java WeatherStation

Displays can be written to user's preference by implementing .update(float temperature, float humidity, float pressure)
method of weather station as well as .display().
